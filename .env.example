# Flask Configuration
SECRET_KEY=your-secret-key-here-change-this-in-production
FLASK_ENV=production

# Database Configuration
# For Railway, the SQLite database will be stored in the volume
# DATABASE_URL will be automatically set by Railway if using PostgreSQL
# If using SQLite (default), it will be stored in /data/bms_tool.db

# Server Configuration
PORT=5001
